import {Meta, StoryObj} from '@storybook/react';
import AllPaymentCard from '.';

const meta: Meta<typeof AllPaymentCard> = {
	title: 'Organisms/AllPaymentCard',
	component: AllPaymentCard,
};
type Story = StoryObj<typeof AllPaymentCard>;

export const Example: Story = {

};

export default meta;
