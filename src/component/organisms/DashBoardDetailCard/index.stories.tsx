import {Meta, StoryObj} from '@storybook/react';
import DashBoardDetailCard from '.';

const meta: Meta<typeof DashBoardDetailCard> = {
	title: 'Organisms/DashBoardDetailCard',
	component: DashBoardDetailCard,
};

type Story = StoryObj<typeof DashBoardDetailCard>;

export const Example: Story = {};

export default meta;
