import {Meta, StoryObj} from '@storybook/react';
import CongratesCard from '.';

const meta: Meta<typeof CongratesCard> = {
	title: 'Organisms/CongratesCard',
	component: CongratesCard,
};

type Story = StoryObj<typeof CongratesCard>;

export const Example: Story = {};

export default meta;
